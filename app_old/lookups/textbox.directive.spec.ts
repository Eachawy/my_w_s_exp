import { Textboxcomponent } from './textbox.directive';

describe('TextboxDirective', () => {
  it('should create an instance', () => {
    const directive = new Textboxcomponent();
    expect(directive).toBeTruthy();
  });
});
