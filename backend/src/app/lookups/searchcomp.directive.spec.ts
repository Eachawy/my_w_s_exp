import { Searchcomp } from './searchcomp.directive';

describe('SearchcompDirective', () => {
  it('should create an instance', () => {
    const directive = new Searchcomp();
    expect(directive).toBeTruthy();
  });
});
